//biblioteca para dados da unidade (Obrigatória para funcionamento do sistema final "ultimo")
#include <stdio.h>
#include <stdlib.h>

//definição
typedef struct unidade
{
    char nomedaunidade[100];
    char localizacao[100];
    char horariodefunc[50];
    char telefonecontato[50];

}unidade;
